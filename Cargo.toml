[package]
name = "pixel-paint-studio"
version = "0.3.0"
edition = "2021"

[dependencies]
image = { version = "0.25", default-features = false, features = ["jpeg", "png"] }
macroquad = "0.4.14"
rfd = "0.15"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
